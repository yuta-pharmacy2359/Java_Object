package exercise;

public class Quiz6_1_1 {
	/*
	  
	 A ← B ← C
	 A ← B ← D ← E 
	 
	 間違っているもの
	 (1) CはAのサブクラスである → x
	 (2) CはEのスーパークラスである → ◯
	 (3) EはAのサブクラスである → x
	 (4) BはEのスーパークラスである → x
	 (5) AはDのスーパークラスである → x
	  
	 */
}